import { DifficultyColorDirective } from './difficulty-color.directive';

describe('DifficultyColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DifficultyColorDirective();
    expect(directive).toBeTruthy();
  });
});
